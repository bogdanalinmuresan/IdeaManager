import { SplitAndGetLastPipe } from './split-and-get-last.pipe';

describe('SplitAndGetLastPipe', () => {
  it('create an instance', () => {
    const pipe = new SplitAndGetLastPipe();
    expect(pipe).toBeTruthy();
  });
});
